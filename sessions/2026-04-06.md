# Session Handoff — Systemix — 2026-04-06

## What Was Decided

- **Pivot confirmed:** Workspace canvas is the product. Landing page = CLI concept (`npx systemix init`). ADR-005.
- **OKLCH as color standard:** Full hex→oklch math pipeline built. ADR-002.
- **Figma naming convention verified:** verolab uses `base/*` → `--*` maps 1:1. ADR-003.
- **Workspace canvas pattern locked:** Collapsible left sidebar, one canvas page per component. ADR-001.
- **Data fetched at build time (for now):** Figma values hardcoded in `variables.ts` with fetch timestamp. ADR-004.

## What Was Produced

| File | Description |
|---|---|
| `src/lib/utils/color.ts` | hex→OKLCH converter (Björn Ottosson matrices), deltaE, driftLabel |
| `src/lib/data/variables.ts` | 47 tokens with real Figma values from verolab kit (fetched 2026-04-06) |
| `src/components/ui/popover.tsx` | Radix Popover wrapper |
| `src/components/ui/command.tsx` | cmdk Command wrapper |
| `src/components/ui/combobox.tsx` | Full Combobox component |
| `src/app/workspace/layout.tsx` | WorkspaceShell — collapsible left nav |
| `src/app/workspace/variables/page.tsx` | Variables canvas — 47 tokens, drift badges, hex→oklch display |
| `src/app/workspace/combobox/page.tsx` | Combobox canvas — live preview, critical drift banner, token audit |
| `src/app/workspace/page.tsx` | Redirect → /workspace/variables |
| `src/app/page.tsx` | Landing page rewrite — "Extract once. Build forever." |
| `decisions/ADR.md` | 5 ADRs recorded |
| `sessions/2026-04-06.md` | This file |
| `PROJECT_SUMMARY.md` | 500-word project summary |

## What's Still Open

| Item | Owner | Linear |
|---|---|---|
| Figma URL input → dynamic token fetch | Boyan | BAST-110 |
| Scaffold figma-plugin package | Boyan | BAST-111 |
| Cut fake dashboard pages from nav | Boyan | BAST-106 |
| Push tokens to Figma (`/sync-to-figma`) | Boyan | — |
| Capture `/pipeline` page to Figma | Boyan | — |

## ADRs Recorded

- ADR-001: Workspace Canvas Pattern
- ADR-002: OKLCH as the Internal Color Standard
- ADR-003: Figma Variable Naming Convention
- ADR-004: Figma Data Fetched at Build Time
- ADR-005: Pivot — Workspace Canvas Over Marketing Site

## Risks Surfaced

- **Token staleness:** `variables.ts` Figma values are from 2026-04-06. Any verolab kit update won't be reflected until a re-fetch. The fetch timestamp in the file is the only indicator.
- **3 critical drift tokens:** `--accent`, `--border`, `--input` are significantly different from Figma. Selected state on Combobox looks visually wrong when rendered with the code tokens. Needs a design decision: align code to Figma or document the intentional deviation.
- **cmdk dependency:** Installed manually (`npm install cmdk`). Not in the original package.json — verify it's tracked correctly.

## Next Session Focus

Start with BAST-110: wire up the "Paste Figma URL" input in the variables page header to fetch live variable data via `mcp__claude_ai_Figma__get_variable_defs` and populate the Figma column dynamically instead of relying on the hardcoded `FIGMA_VALUES` map.
