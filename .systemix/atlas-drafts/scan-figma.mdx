---
id: scan-figma
persona: designer
title: "Extract design context and screenshot from a Figma URL"
pattern: chain
surface: desktop
problem: "Extract design context and screenshot from a Figma URL"
steps:
  - { id: trigger, label: Trigger, kind: input, note: "Invoke /figma" }
  - { id: process, label: Process, kind: agent, note: "Extract design context and screenshot from a Figma URL", agent: flux }
  - { id: output, label: Done, kind: output, note: "Skill complete" }
edges:
  - { from: trigger, to: process }
  - { from: process, to: output }
---

# Extract design context and screenshot from a Figma URL

Extract design context and screenshot from a Figma URL

> Auto-drafted by `npx systemix atlas scan` from skill `/figma`.
> Review steps, edges, and labels before approving — then approve in the queue to publish.
