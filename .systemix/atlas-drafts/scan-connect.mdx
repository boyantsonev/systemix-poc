---
id: scan-connect
persona: designer
title: "Link Figma components to codebase React components bidirecti"
pattern: chain
surface: desktop
problem: "Link Figma components to codebase React components bidirectionally — updates Code Connect in Figma and adds implementation notes to Figma component descriptions."
steps:
  - { id: trigger, label: Trigger, kind: input, note: "Invoke /connect" }
  - { id: process, label: Process, kind: agent, note: "Link Figma components to codebase React components bidirectionally — updates Cod", agent: flux }
  - { id: output, label: Done, kind: output, note: "Skill complete" }
edges:
  - { from: trigger, to: process }
  - { from: process, to: output }
---

# Link Figma components to codebase React components bidirecti

Link Figma components to codebase React components bidirectionally — updates Code Connect in Figma and adds implementation notes to Figma component descriptions.

> Auto-drafted by `npx systemix atlas scan` from skill `/connect`.
> Review steps, edges, and labels before approving — then approve in the queue to publish.
