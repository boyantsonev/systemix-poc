// Single source of truth for cross-site constants. Swap githubUrl here when the
// repo moves to an org (decision #5, site-rebuild blueprint).
export const siteConfig = {
  githubUrl: "https://github.com/boyantsonev/systemix",
  docsUrl: "/docs",
} as const;
