# Learnings

The synthesized memory of this instance's loop. Every entry is **earned**: it is
appended by `/close-experiment` when an experiment resolves, cites the experiment
that produced it (provenance), and carries a confidence and a review-by date.
Newest first. Nothing is hand-written.

## Memory

*No entries yet.* The first entry lands when the first experiment closes.
