{
  "name": "systemix-mcp-proxy",
  "version": "0.1.0",
  "description": "Token-optimizing MCP proxy for Figma — intercepts and optimizes MCP calls",
  "license": "MIT",
  "bin": {
    "systemix-mcp-proxy": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  }
}
